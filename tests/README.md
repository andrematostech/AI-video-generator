# Tests

This project uses Vitest for local automated tests.

Run all tests with:

- `npm test`

Run in watch mode with:

- `npm run test:watch`

Test environment files are loaded in this order when present:

- `.env.test.local`
- `.env.test`
- `.env`
